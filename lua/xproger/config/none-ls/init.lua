local goimports_reviser = require("xproger.config.none-ls.goimports_reviser")
local cspell = require("xproger.config.none-ls.cspell")
local config = {
    ["goimports_reviser"] = goimports_reviser,
    ["cspell"] = cspell,
}

return config
